{
  "name": "mcp-code-review-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for MCP Code Review system",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:webhook": "playwright test webhook-background-task.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}